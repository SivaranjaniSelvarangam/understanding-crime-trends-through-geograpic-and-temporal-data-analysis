version: 0.1

component: build

steps:
  - type: Command
    name: "Install"
    command: echo "Installing..."

  - type: Command
    name: "Build"
    command: echo "Building project..."

  - type: Command
    name: "Run"
    command: echo "Running project..."
